# Conventions
- TypeScript is used for type safety. Ensure proper type hints and interfaces are defined in `types.ts` or near usage.
- Styling is handled via Tailwind CSS classes directly on elements.
- Uses Vite for fast builds and React 19 features.
- Code should remain strictly a mock/practice tool (no live prompters during actual interviews).
- **CRITICAL STANDING INSTRUCTION**: AI Assistants (like Antigravity) must prioritise using Serena MCP tools where possible for file edits, reading, and analysis.