# Suggested Commands
- Dev Server: `npm run dev`
- Build: `npm run build`
- Preview: `npm run preview`
- Install dependencies: `npm install`
Note: For environment variables, create `.env.local` using `cp .env.example .env.local` (or `copy` on Windows cmd) and add `GEMINI_API_KEY`.